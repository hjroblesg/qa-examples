"""container-test-framework (ctf).

A small, container-agnostic engine for validating containers, plus a pluggable
validator layer so the same core drives MySQL, REST API and (later) deep-learning
benchmark checks.
"""
__version__ = "0.1.0"
