# Runtime dependencies for the framework itself.
# The engine shells out to the `docker` CLI and uses only the standard library.

# MySQL validator driver (imported lazily; only needed for real/integration runs,
# not for the mocked unit tests).
mysql-connector-python>=8.3

# The functional/integration layer (tests/*.robot) uses Robot Framework.
robotframework>=6.1
robotframework-requests>=0.9
# MySQL suite (tests/mysql_tests.robot): DB keywords + driver.
# Pinned to DatabaseLibrary 1.x for a stable keyword set (2.x deprecates the
# classic Check If Exists / Row Count keywords used here).
robotframework-databaselibrary>=1.4,<2
pymysql>=1.1
